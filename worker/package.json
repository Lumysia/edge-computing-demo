{
  "name": "edge-computing-demo",
  "version": "1.0.0",
  "description": "Edge Computing Demo — Edge Processing vs Cloud Round-Trip (Shi et al. 2016 + Cloudflare Workers)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^6.0.2",
    "wrangler": "^3.80.0"
  }
}
